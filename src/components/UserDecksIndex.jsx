import DeckDisplay from "./DeckDisplay.jsx";

const UserDecksIndex = () => {

    return (

        // for each deck create a display... idk how I'm gonna do this
        // perhaps the strat is to show the first few cards for each deck or something like spotify - make an image of the first few cards - stack of cards
        // then you can click on the deck and it show the legit deck grid or something 
        <div class="">
            <DeckDisplay deckId={'65ebe3992debe412aff0ff8e'} />
        </div>
    );
}
export default UserDecksIndex;